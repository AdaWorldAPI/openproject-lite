# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/openproject

# Redis (for sessions + queues)
REDIS_URL=redis://localhost:6379

# Auth
SESSION_SECRET=change-me-to-something-random-in-production

# MS Graph Mail
MSGRAPH_TENANT_ID=
MSGRAPH_CLIENT_ID=
MSGRAPH_CLIENT_SECRET=
MSGRAPH_SENDER_EMAIL=
MSGRAPH_SENDER_NAME=OpenProject-Lite

# App
PORT=3000
NODE_ENV=development
